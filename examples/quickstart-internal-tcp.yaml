# AxonOps TCP healthchecks — safe to kubectl delete -f this file.
# Requires quickstart-internal-connection.yaml to be applied first.
# kubectl apply -f examples/quickstart-internal-tcp.yaml
# kubectl delete -f examples/quickstart-internal-tcp.yaml

---
apiVersion: alerts.axonops.com/v1alpha1
kind: AxonOpsHealthcheckTCP
metadata:
  name: cassandra-cql-tcp
  namespace: default
spec:
  connectionRef: example
  clusterName: test-cluster-000
  clusterType: cassandra
  name: "Cassandra CQL Port Check"
  tcp: "0.0.0.0:9042"
  interval: "1m"
  timeout: "30s"
  readonly: false

---
apiVersion: alerts.axonops.com/v1alpha1
kind: AxonOpsHealthcheckTCP
metadata:
  name: cassandra-jmx-tcp
  namespace: default
spec:
  connectionRef: example
  clusterName: test-cluster-000
  clusterType: cassandra
  name: "Cassandra JMX Port Check"
  tcp: "0.0.0.0:7199"
  interval: "2m"
  timeout: "30s"
  readonly: true

---
apiVersion: alerts.axonops.com/v1alpha1
kind: AxonOpsHealthcheckTCP
metadata:
  name: cassandra-storage-tcp
  namespace: default
spec:
  connectionRef: example
  clusterName: test-cluster-000
  clusterType: cassandra
  name: "Cassandra Storage Port Check"
  tcp: "0.0.0.0:7000"
  interval: "1m"
  timeout: "30s"
  readonly: true

---
apiVersion: alerts.axonops.com/v1alpha1
kind: AxonOpsHealthcheckTCP
metadata:
  name: cassandra-ssl-storage-tcp
  namespace: default
spec:
  connectionRef: example
  clusterName: test-cluster-000
  clusterType: cassandra
  name: "Cassandra SSL Storage Port Check"
  tcp: "0.0.0.0:7001"
  interval: "2m"
  timeout: "30s"
  readonly: true

---
apiVersion: alerts.axonops.com/v1alpha1
kind: AxonOpsHealthcheckTCP
metadata:
  name: cassandra-native-tcp
  namespace: default
spec:
  connectionRef: example
  clusterName: test-cluster-000
  clusterType: cassandra
  name: "Cassandra Native Transport Port Check"
  tcp: "0.0.0.0:9042"
  interval: "1m"
  timeout: "30s"
  readonly: false
