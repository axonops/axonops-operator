Thank you for installing {{ .Chart.Name }} {{ .Chart.Version }}.

Your release is named {{ .Release.Name }} and is running in namespace {{ .Release.Namespace }}.

To verify the operator is running:

  kubectl get pods -n {{ .Release.Namespace }}

To verify the CRDs were installed:

  kubectl get customresourcedefinitions | grep axonops.com

Next steps:

  1. Create an AxonOpsConnection with your API credentials:

       kubectl apply -f https://raw.githubusercontent.com/axonops/axonops-operator/main/config/samples/core_v1alpha1_axonopsconnection.yaml

  2. Deploy the full AxonOps stack with an AxonOpsPlatform:

       kubectl apply -f https://raw.githubusercontent.com/axonops/axonops-operator/main/config/samples/core_v1alpha1_axonopsplatform.yaml

  3. Or apply all sample resources at once:

       kubectl apply -k https://github.com/axonops/axonops-operator/config/samples

For full documentation and examples visit:
  https://github.com/axonops/axonops-operator

To check the release status:

  helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
