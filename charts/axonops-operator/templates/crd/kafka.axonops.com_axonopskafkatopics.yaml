---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: axonopskafkatopics.kafka.axonops.com
spec:
  group: kafka.axonops.com
  names:
    kind: AxonOpsKafkaTopic
    listKind: AxonOpsKafkaTopicList
    plural: axonopskafkatopics
    shortNames:
    - axkt
    singular: axonopskafkatopic
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - jsonPath: .spec.name
      name: Topic
      type: string
    - jsonPath: .spec.partitions
      name: Partitions
      type: integer
    - jsonPath: .spec.replicationFactor
      name: RF
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AxonOpsKafkaTopic is the Schema for the axonopskafkatopics API.
          Manages Kafka topics through the AxonOps API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AxonOpsKafkaTopicSpec defines the desired state of AxonOpsKafkaTopic.
            properties:
              clusterName:
                description: ClusterName is the Kafka cluster name in AxonOps
                minLength: 1
                type: string
              config:
                additionalProperties:
                  type: string
                description: |-
                  Config contains Kafka topic configuration overrides.
                  Keys use dot notation (e.g., "cleanup.policy", "retention.ms").
                  Only explicitly set configs are managed; unset keys use Kafka broker defaults.
                type: object
              connectionRef:
                description: ConnectionRef is the name of an AxonOpsConnection in
                  the same namespace.
                minLength: 1
                type: string
              name:
                description: Name is the Kafka topic name. Immutable after creation.
                minLength: 1
                type: string
              partitions:
                description: Partitions is the number of partitions. Immutable after
                  creation.
                format: int32
                minimum: 1
                type: integer
              replicationFactor:
                description: ReplicationFactor is the replication factor. Immutable
                  after creation.
                format: int32
                minimum: 1
                type: integer
            required:
            - clusterName
            - connectionRef
            - name
            - partitions
            - replicationFactor
            type: object
          status:
            description: AxonOpsKafkaTopicStatus defines the observed state of AxonOpsKafkaTopic.
            properties:
              conditions:
                description: Conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime is the timestamp of the last successful
                  sync
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation most recently
                  observed
                format: int64
                type: integer
              synced:
                description: Synced indicates the topic has been created in the Kafka
                  cluster
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
