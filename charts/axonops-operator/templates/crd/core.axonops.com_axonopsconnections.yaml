---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: axonopsconnections.core.axonops.com
spec:
  group: core.axonops.com
  names:
    kind: AxonOpsConnection
    listKind: AxonOpsConnectionList
    plural: axonopsconnections
    shortNames:
    - axconn
    singular: axonopsconnection
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.orgId
      name: OrgID
      type: string
    - jsonPath: .spec.host
      name: Host
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AxonOpsConnection is the Schema for the axonopsconnections API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AxonOpsConnection
            properties:
              apiKeyRef:
                description: APIKeyRef references a Secret containing the API key
                properties:
                  key:
                    description: 'Key is the key in the Secret containing the API
                      key (default: "api_key")'
                    type: string
                  name:
                    description: Name is the name of the Secret in the same namespace
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              host:
                description: |-
                  Host is the AxonOps server hostname/URL (e.g., "dash.axonops.cloud" or "axonops.example.com")
                  If not provided, defaults to SaaS (dash.axonops.cloud)
                type: string
              orgId:
                description: OrgID is the organization ID for AxonOps
                minLength: 1
                type: string
              protocol:
                description: |-
                  Protocol is the protocol to use (http or https)
                  If not provided, defaults to https
                enum:
                - http
                - https
                type: string
              timeout:
                description: |-
                  Timeout is the HTTP client timeout for API requests (e.g. "30s", "2m").
                  If not provided, defaults to 30s. Recommended values: 30s for low-latency
                  networks, 60s-120s for cross-region or high-latency connections.
                type: string
              tlsSkipVerify:
                description: |-
                  TLSSkipVerify disables TLS certificate verification
                  Should only be used for testing
                type: boolean
              tokenType:
                description: |-
                  TokenType is the token type for authentication (Bearer or AxonApi)
                  If not provided, defaults to Bearer
                enum:
                - Bearer
                - AxonApi
                type: string
              useSaml:
                description: |-
                  UseSAML indicates if SAML is enabled (changes host URL pattern)
                  If not provided, defaults to false
                type: boolean
            required:
            - apiKeyRef
            - orgId
            type: object
          status:
            description: status defines the observed state of AxonOpsConnection
            properties:
              conditions:
                description: |-
                  Conditions represent the current state of the AxonOpsConnection resource
                  Standard condition types include:
                  - "Ready": the connection is valid and secret is readable
                  - "Failed": the connection failed (e.g., secret not found)
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastValidationTime:
                description: LastValidationTime is the timestamp of the last successful
                  validation
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed AxonOpsConnection
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
