---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: axonopsscheduledrepairs.alerts.axonops.com
spec:
  group: alerts.axonops.com
  names:
    kind: AxonOpsScheduledRepair
    listKind: AxonOpsScheduledRepairList
    plural: axonopsscheduledrepairs
    shortNames:
    - axrep
    singular: axonopsscheduledrepair
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - jsonPath: .spec.tag
      name: Tag
      type: string
    - jsonPath: .spec.scheduleExpression
      name: Schedule
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AxonOpsScheduledRepair is the Schema for the axonopsscheduledrepairs API.
          Manages Cassandra scheduled repairs through the AxonOps API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AxonOpsScheduledRepairSpec defines the desired state of AxonOpsScheduledRepair.
              Manages Cassandra scheduled repair configurations through the AxonOps API.
            properties:
              blacklistedTables:
                description: BlacklistedTables are tables to exclude from repair.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              clusterName:
                description: ClusterName is the Cassandra/DSE cluster name in AxonOps
                minLength: 1
                type: string
              clusterType:
                description: ClusterType is the cluster type. Only cassandra and dse
                  support scheduled repairs.
                enum:
                - cassandra
                - dse
                type: string
              connectionRef:
                description: ConnectionRef is the name of an AxonOpsConnection in
                  the same namespace.
                minLength: 1
                type: string
              incremental:
                description: Incremental enables incremental repair.
                type: boolean
              jobThreads:
                default: 1
                description: JobThreads is the number of repair threads.
                minimum: 1
                type: integer
              keyspace:
                description: Keyspace to repair. Empty means all keyspaces.
                type: string
              nodes:
                description: Nodes limits repair to specific nodes. Empty means all
                  nodes.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              optimiseStreams:
                description: OptimiseStreams enables repair stream optimisation.
                type: boolean
              parallelism:
                default: Parallel
                description: Parallelism controls the repair execution mode.
                enum:
                - Parallel
                - Sequential
                - DC-Aware
                type: string
              paxosOnly:
                description: PaxosOnly runs Paxos repair only. Mutually exclusive
                  with SkipPaxos.
                type: boolean
              primaryRange:
                description: PrimaryRange enables primary range repair only.
                type: boolean
              scheduleExpression:
                description: ScheduleExpression is the cron expression for scheduling
                  (e.g., "0 0 1 * *" for monthly)
                minLength: 1
                type: string
              segmented:
                description: Segmented enables segmented repair.
                type: boolean
              segmentsPerNode:
                default: 1
                description: SegmentsPerNode is the number of segments per node for
                  segmented repair.
                minimum: 1
                type: integer
              skipPaxos:
                description: SkipPaxos skips Paxos repair. Mutually exclusive with
                  PaxosOnly.
                type: boolean
              specificDataCenters:
                description: SpecificDataCenters limits repair to specific data centers.
                  Empty means all.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              tables:
                description: Tables to repair within the keyspace. Empty means all
                  tables.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              tag:
                description: Tag uniquely identifies this repair configuration within
                  the cluster.
                minLength: 1
                type: string
            required:
            - clusterName
            - clusterType
            - connectionRef
            - scheduleExpression
            - tag
            type: object
          status:
            description: AxonOpsScheduledRepairStatus defines the observed state of
              AxonOpsScheduledRepair.
            properties:
              conditions:
                description: Conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime is the timestamp of the last successful
                  sync
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation most recently
                  observed
                format: int64
                type: integer
              syncedRepairID:
                description: SyncedRepairID is the ID assigned by the AxonOps API
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
