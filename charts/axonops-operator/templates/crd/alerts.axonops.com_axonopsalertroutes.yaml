---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: axonopsalertroutes.alerts.axonops.com
spec:
  group: alerts.axonops.com
  names:
    kind: AxonOpsAlertRoute
    listKind: AxonOpsAlertRouteList
    plural: axonopsalertroutes
    singular: axonopsalertroute
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.severity
      name: Severity
      type: string
    - jsonPath: .spec.integrationName
      name: Integration
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AxonOpsAlertRoute is the Schema for the axonopsalertroutes API.
          Routes alert notifications from AxonOps to integrations (Slack, PagerDuty, email, etc.)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AxonOpsAlertRoute
            properties:
              clusterName:
                description: ClusterName is the name of the cluster
                type: string
              clusterType:
                description: ClusterType is the cluster type (cassandra, kafka, or
                  dse)
                enum:
                - cassandra
                - kafka
                - dse
                type: string
              connectionRef:
                description: ConnectionRef references the AxonOpsConnection for API
                  credentials
                type: string
              enableOverride:
                default: true
                description: |-
                  EnableOverride enables override for non-global routes.
                  Ignored for global routes. Defaults to true.
                type: boolean
              integrationName:
                description: IntegrationName is the name of the integration
                type: string
              integrationType:
                description: IntegrationType is the type of integration
                enum:
                - email
                - smtp
                - pagerduty
                - slack
                - teams
                - servicenow
                - webhook
                - opsgenie
                type: string
              severity:
                description: Severity is the alert severity level
                enum:
                - info
                - warning
                - error
                type: string
              type:
                description: Type is the route type
                enum:
                - global
                - metrics
                - backups
                - servicechecks
                - nodes
                - commands
                - repairs
                - rollingrestart
                type: string
            required:
            - clusterName
            - clusterType
            - connectionRef
            - integrationName
            - integrationType
            - severity
            - type
            type: object
          status:
            description: status defines the observed state of AxonOpsAlertRoute
            properties:
              conditions:
                description: Conditions represent the current state of the AxonOpsAlertRoute
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              integrationID:
                description: IntegrationID is the resolved ID of the integration from
                  the AxonOps API
                type: string
              lastSyncTime:
                description: LastSyncTime is the last time the route was successfully
                  synced with the AxonOps API
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation observed by the
                  controller
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
