---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: axonopsplatforms.core.axonops.com
spec:
  group: core.axonops.com
  names:
    kind: AxonOpsPlatform
    listKind: AxonOpsPlatformList
    plural: axonopsplatforms
    singular: axonopsplatform
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .spec.pause
      name: Paused
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AxonOpsPlatform is the Schema for the axonopsplatforms API; it's
          the top-level resource for the AxonOps observability platform (server, dashboard,
          TimeSeries and SearchDB)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AxonOpsPlatform
            properties:
              dashboard:
                description: Dashboard configures the axon-dash component
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the component pods
                    type: object
                  authentication:
                    description: |-
                      Authentication configures database credentials.
                      If not specified, random credentials will be generated automatically.
                    properties:
                      password:
                        description: |-
                          Password for database authentication. If empty and SecretRef is not set,
                          a random password will be generated.
                        type: string
                      secretRef:
                        description: |-
                          SecretRef references an existing Secret containing AXONOPS_DB_USER and AXONOPS_DB_PASSWORD keys.
                          If set, Username and Password fields are ignored.
                        type: string
                      username:
                        description: |-
                          Username for database authentication. If empty and SecretRef is not set,
                          a random username will be generated.
                        type: string
                    type: object
                  enabled:
                    default: true
                    description: |-
                      Enabled determines if the component should be deployed.
                      Defaults to true when the component is specified.
                    type: boolean
                  env:
                    description: Env is a list of additional environment variables
                    x-kubernetes-preserve-unknown-fields: true
                  external:
                    description: External configures external access to the component
                    properties:
                      dataCenter:
                        description: |-
                          DataCenter is the Cassandra datacenter name for CQL connections.
                          When set on an external TimeSeries component, it is used in
                          cql_local_dc and cql_keyspace_replication. Defaults to "axonopsdb_dc1".
                        type: string
                      hosts:
                        description: Hosts is a list of external hostnames for the
                          component
                        items:
                          type: string
                        type: array
                      tls:
                        description: TLS configures TLS settings for external connections
                        properties:
                          certSecretRef:
                            description: |-
                              CertSecretRef is the name of a Secret containing TLS certificates for
                              this external connection. Required when Enabled is true and
                              InsecureSkipVerify is false. The Secret must contain the keys:
                              ca.crt, tls.crt, tls.key.
                            type: string
                          enabled:
                            description: Enabled enables TLS for connections
                            type: boolean
                          insecureSkipVerify:
                            description: |-
                              InsecureSkipVerify skips TLS certificate verification.
                              When false (the default), CertSecretRef must be set so the operator
                              can mount the CA and client certificates into the Server pod.
                            type: boolean
                        type: object
                    type: object
                  extraVolumeMounts:
                    description: ExtraVolumeMounts defines additional volume mounts
                    x-kubernetes-preserve-unknown-fields: true
                  extraVolumes:
                    description: ExtraVolumes defines additional volumes to mount
                    x-kubernetes-preserve-unknown-fields: true
                  gateway:
                    description: Gateway configures GatewayAPI for dashboard access
                    x-kubernetes-preserve-unknown-fields: true
                  heapSize:
                    default: 1024M
                    description: HeapSize configures the JVM heap size (e.g., "1024M",
                      "4G")
                    type: string
                  ingress:
                    description: Ingress configures ingress for dashboard access
                    x-kubernetes-preserve-unknown-fields: true
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to add to the component pods
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: |-
                      NodeSelector is a map of key-value pairs used to constrain pods to nodes
                      with matching labels. See https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/
                    type: object
                  replicas:
                    default: 1
                    x-kubernetes-preserve-unknown-fields: true
                  repository:
                    description: Repository configures the container image
                    properties:
                      image:
                        description: Image is the container image name
                        type: string
                      pullPolicy:
                        description: PullPolicy is the image pull policy (e.g., "Always",
                          "IfNotPresent", "Never")
                        type: string
                      tag:
                        description: Tag is the container image tag
                        type: string
                    type: object
                  resources:
                    description: Resources defines compute resources for the component
                    x-kubernetes-preserve-unknown-fields: true
                  storageConfig:
                    description: StorageConfig defines persistent storage configuration
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              imageRegistry:
                description: |-
                  ImageRegistry overrides the registry host for all default component images.
                  When set, the registry portion of each default image (e.g., "ghcr.io" in
                  "ghcr.io/axonops/axondb-timeseries") is replaced with this value.
                  Per-component repository.image overrides take full precedence over this field.
                  Must not include a URI scheme (no "https://"). May include a port (e.g., "harbor.internal.com:5000").
                type: string
              initImage:
                description: |-
                  InitImage is the container image used for init containers (e.g., volume permission fixing).
                  Defaults to "docker.io/library/busybox:1.37.0" if not specified. Must be pinned to a specific version.
                type: string
              pause:
                default: false
                description: |-
                  Pause stops the controller from reconciling this AxonOpsPlatform resource.
                  Existing owned resources (StatefulSets, Deployments, Services, Secrets,
                  Certificates) are left untouched. Deletion still proceeds normally.
                type: boolean
              search:
                description: Search configures the axondb-search component
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the component pods
                    type: object
                  authentication:
                    description: |-
                      Authentication configures database credentials.
                      If not specified, random credentials will be generated automatically.
                    properties:
                      password:
                        description: |-
                          Password for database authentication. If empty and SecretRef is not set,
                          a random password will be generated.
                        type: string
                      secretRef:
                        description: |-
                          SecretRef references an existing Secret containing AXONOPS_DB_USER and AXONOPS_DB_PASSWORD keys.
                          If set, Username and Password fields are ignored.
                        type: string
                      username:
                        description: |-
                          Username for database authentication. If empty and SecretRef is not set,
                          a random username will be generated.
                        type: string
                    type: object
                  enabled:
                    default: true
                    description: |-
                      Enabled determines if the component should be deployed.
                      Defaults to true when the component is specified.
                    type: boolean
                  env:
                    description: Env is a list of additional environment variables
                    x-kubernetes-preserve-unknown-fields: true
                  external:
                    description: External configures external access to the component
                    properties:
                      dataCenter:
                        description: |-
                          DataCenter is the Cassandra datacenter name for CQL connections.
                          When set on an external TimeSeries component, it is used in
                          cql_local_dc and cql_keyspace_replication. Defaults to "axonopsdb_dc1".
                        type: string
                      hosts:
                        description: Hosts is a list of external hostnames for the
                          component
                        items:
                          type: string
                        type: array
                      tls:
                        description: TLS configures TLS settings for external connections
                        properties:
                          certSecretRef:
                            description: |-
                              CertSecretRef is the name of a Secret containing TLS certificates for
                              this external connection. Required when Enabled is true and
                              InsecureSkipVerify is false. The Secret must contain the keys:
                              ca.crt, tls.crt, tls.key.
                            type: string
                          enabled:
                            description: Enabled enables TLS for connections
                            type: boolean
                          insecureSkipVerify:
                            description: |-
                              InsecureSkipVerify skips TLS certificate verification.
                              When false (the default), CertSecretRef must be set so the operator
                              can mount the CA and client certificates into the Server pod.
                            type: boolean
                        type: object
                    type: object
                  extraVolumeMounts:
                    description: ExtraVolumeMounts defines additional volume mounts
                    x-kubernetes-preserve-unknown-fields: true
                  extraVolumes:
                    description: ExtraVolumes defines additional volumes to mount
                    x-kubernetes-preserve-unknown-fields: true
                  heapSize:
                    default: 1024M
                    description: HeapSize configures the JVM heap size (e.g., "1024M",
                      "4G")
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to add to the component pods
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: |-
                      NodeSelector is a map of key-value pairs used to constrain pods to nodes
                      with matching labels. See https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/
                    type: object
                  repository:
                    description: Repository configures the container image
                    properties:
                      image:
                        description: Image is the container image name
                        type: string
                      pullPolicy:
                        description: PullPolicy is the image pull policy (e.g., "Always",
                          "IfNotPresent", "Never")
                        type: string
                      tag:
                        description: Tag is the container image tag
                        type: string
                    type: object
                  resources:
                    description: Resources defines compute resources for the component
                    x-kubernetes-preserve-unknown-fields: true
                  storageConfig:
                    description: StorageConfig defines persistent storage configuration
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              server:
                description: Server configures the axon-server component
                properties:
                  agentGateway:
                    description: AgentGateway configures gateway for agent connections
                    x-kubernetes-preserve-unknown-fields: true
                  agentIngress:
                    description: AgentIngress configures ingress for agent connections
                    x-kubernetes-preserve-unknown-fields: true
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the component pods
                    type: object
                  apiGateway:
                    description: ApiGateway configures ingress for API access
                    x-kubernetes-preserve-unknown-fields: true
                  apiIngress:
                    description: ApiIngress configures ingress for API access
                    x-kubernetes-preserve-unknown-fields: true
                  authentication:
                    description: |-
                      Authentication configures database credentials.
                      If not specified, random credentials will be generated automatically.
                    properties:
                      password:
                        description: |-
                          Password for database authentication. If empty and SecretRef is not set,
                          a random password will be generated.
                        type: string
                      secretRef:
                        description: |-
                          SecretRef references an existing Secret containing AXONOPS_DB_USER and AXONOPS_DB_PASSWORD keys.
                          If set, Username and Password fields are ignored.
                        type: string
                      username:
                        description: |-
                          Username for database authentication. If empty and SecretRef is not set,
                          a random username will be generated.
                        type: string
                    type: object
                  config:
                    description: |-
                      Config provides additional settings to append to the axon-server configuration.
                      Any valid axon-server YAML configuration can be specified here and will be
                      merged into the generated config.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  enabled:
                    default: true
                    description: |-
                      Enabled determines if the component should be deployed.
                      Defaults to true when the component is specified.
                    type: boolean
                  env:
                    description: Env is a list of additional environment variables
                    x-kubernetes-preserve-unknown-fields: true
                  external:
                    description: External configures external access to the component
                    properties:
                      dataCenter:
                        description: |-
                          DataCenter is the Cassandra datacenter name for CQL connections.
                          When set on an external TimeSeries component, it is used in
                          cql_local_dc and cql_keyspace_replication. Defaults to "axonopsdb_dc1".
                        type: string
                      hosts:
                        description: Hosts is a list of external hostnames for the
                          component
                        items:
                          type: string
                        type: array
                      tls:
                        description: TLS configures TLS settings for external connections
                        properties:
                          certSecretRef:
                            description: |-
                              CertSecretRef is the name of a Secret containing TLS certificates for
                              this external connection. Required when Enabled is true and
                              InsecureSkipVerify is false. The Secret must contain the keys:
                              ca.crt, tls.crt, tls.key.
                            type: string
                          enabled:
                            description: Enabled enables TLS for connections
                            type: boolean
                          insecureSkipVerify:
                            description: |-
                              InsecureSkipVerify skips TLS certificate verification.
                              When false (the default), CertSecretRef must be set so the operator
                              can mount the CA and client certificates into the Server pod.
                            type: boolean
                        type: object
                    type: object
                  extraVolumeMounts:
                    description: ExtraVolumeMounts defines additional volume mounts
                    x-kubernetes-preserve-unknown-fields: true
                  extraVolumes:
                    description: ExtraVolumes defines additional volumes to mount
                    x-kubernetes-preserve-unknown-fields: true
                  heapSize:
                    default: 1024M
                    description: HeapSize configures the JVM heap size (e.g., "1024M",
                      "4G")
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to add to the component pods
                    type: object
                  license:
                    x-kubernetes-preserve-unknown-fields: true
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: |-
                      NodeSelector is a map of key-value pairs used to constrain pods to nodes
                      with matching labels. See https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/
                    type: object
                  orgName:
                    description: OrgName configures to organization
                    type: string
                  repository:
                    description: Repository configures the container image
                    properties:
                      image:
                        description: Image is the container image name
                        type: string
                      pullPolicy:
                        description: PullPolicy is the image pull policy (e.g., "Always",
                          "IfNotPresent", "Never")
                        type: string
                      tag:
                        description: Tag is the container image tag
                        type: string
                    type: object
                  resources:
                    description: Resources defines compute resources for the component
                    x-kubernetes-preserve-unknown-fields: true
                  storageConfig:
                    description: StorageConfig defines persistent storage configuration
                    x-kubernetes-preserve-unknown-fields: true
                required:
                - orgName
                type: object
              timeSeries:
                description: TimeSeries configures the axondb-timeseries component
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the component pods
                    type: object
                  authentication:
                    description: |-
                      Authentication configures database credentials.
                      If not specified, random credentials will be generated automatically.
                    properties:
                      password:
                        description: |-
                          Password for database authentication. If empty and SecretRef is not set,
                          a random password will be generated.
                        type: string
                      secretRef:
                        description: |-
                          SecretRef references an existing Secret containing AXONOPS_DB_USER and AXONOPS_DB_PASSWORD keys.
                          If set, Username and Password fields are ignored.
                        type: string
                      username:
                        description: |-
                          Username for database authentication. If empty and SecretRef is not set,
                          a random username will be generated.
                        type: string
                    type: object
                  enabled:
                    default: true
                    description: |-
                      Enabled determines if the component should be deployed.
                      Defaults to true when the component is specified.
                    type: boolean
                  env:
                    description: Env is a list of additional environment variables
                    x-kubernetes-preserve-unknown-fields: true
                  external:
                    description: External configures external access to the component
                    properties:
                      dataCenter:
                        description: |-
                          DataCenter is the Cassandra datacenter name for CQL connections.
                          When set on an external TimeSeries component, it is used in
                          cql_local_dc and cql_keyspace_replication. Defaults to "axonopsdb_dc1".
                        type: string
                      hosts:
                        description: Hosts is a list of external hostnames for the
                          component
                        items:
                          type: string
                        type: array
                      tls:
                        description: TLS configures TLS settings for external connections
                        properties:
                          certSecretRef:
                            description: |-
                              CertSecretRef is the name of a Secret containing TLS certificates for
                              this external connection. Required when Enabled is true and
                              InsecureSkipVerify is false. The Secret must contain the keys:
                              ca.crt, tls.crt, tls.key.
                            type: string
                          enabled:
                            description: Enabled enables TLS for connections
                            type: boolean
                          insecureSkipVerify:
                            description: |-
                              InsecureSkipVerify skips TLS certificate verification.
                              When false (the default), CertSecretRef must be set so the operator
                              can mount the CA and client certificates into the Server pod.
                            type: boolean
                        type: object
                    type: object
                  extraVolumeMounts:
                    description: ExtraVolumeMounts defines additional volume mounts
                    x-kubernetes-preserve-unknown-fields: true
                  extraVolumes:
                    description: ExtraVolumes defines additional volumes to mount
                    x-kubernetes-preserve-unknown-fields: true
                  heapSize:
                    default: 1024M
                    description: HeapSize configures the JVM heap size (e.g., "1024M",
                      "4G")
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to add to the component pods
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: |-
                      NodeSelector is a map of key-value pairs used to constrain pods to nodes
                      with matching labels. See https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/
                    type: object
                  repository:
                    description: Repository configures the container image
                    properties:
                      image:
                        description: Image is the container image name
                        type: string
                      pullPolicy:
                        description: PullPolicy is the image pull policy (e.g., "Always",
                          "IfNotPresent", "Never")
                        type: string
                      tag:
                        description: Tag is the container image tag
                        type: string
                    type: object
                  resources:
                    description: Resources defines compute resources for the component
                    x-kubernetes-preserve-unknown-fields: true
                  storageConfig:
                    description: StorageConfig defines persistent storage configuration
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              tls:
                description: TLS configures cert-manager TLS certificate issuance
                  for internal components.
                properties:
                  issuer:
                    description: |-
                      Issuer configures the ClusterIssuer used to sign TLS certificates.
                      If not specified, a default SelfSigned ClusterIssuer named
                      "axonops-selfsigned" is created and used.
                    properties:
                      caSecretRef:
                        description: |-
                          CASecretRef is the name of a Secret containing a CA key-pair
                          (fields "tls.crt" and "tls.key") to use as the CA for a CA-backed
                          ClusterIssuer. When set, the controller creates a CA-type ClusterIssuer
                          instead of a SelfSigned one.
                          The secret must exist in the operator's namespace.
                          Mutually exclusive with Name.
                        type: string
                      name:
                        description: |-
                          Name is the name of an existing ClusterIssuer to use.
                          When set, the controller will NOT create a default ClusterIssuer.
                          Mutually exclusive with CASecretRef.
                        type: string
                    type: object
                type: object
            type: object
          status:
            description: status defines the observed state of AxonOpsPlatform
            properties:
              conditions:
                description: Conditions represent the current state of the AxonOpsPlatform
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration reflects the generation observed by
                  the controller.
                format: int64
                type: integer
              searchCertSecretName:
                description: SearchCertSecretName is the name of the Secret containing
                  the TLS certificate for Search.
                type: string
              searchSecretName:
                description: |-
                  SearchSecretName is the name of the Secret containing Search credentials.
                  This is either the user-provided SecretRef or an auto-generated secret name.
                type: string
              timeSeriesCertSecretName:
                description: TimeSeriesCertSecretName is the name of the Secret containing
                  the TLS certificate for TimeSeries.
                type: string
              timeSeriesSecretName:
                description: |-
                  TimeSeriesSecretName is the name of the Secret containing TimeSeries credentials.
                  This is either the user-provided SecretRef or an auto-generated secret name.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
