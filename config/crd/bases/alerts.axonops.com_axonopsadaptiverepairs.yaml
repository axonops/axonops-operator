---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: axonopsadaptiverepairs.alerts.axonops.com
spec:
  group: alerts.axonops.com
  names:
    kind: AxonOpsAdaptiveRepair
    listKind: AxonOpsAdaptiveRepairList
    plural: axonopsadaptiverepairs
    singular: axonopsadaptiverepair
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - jsonPath: .spec.clusterType
      name: Type
      type: string
    - jsonPath: .spec.active
      name: Active
      type: boolean
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AxonOpsAdaptiveRepair is the Schema for the axonopsadaptiverepairs
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AxonOpsAdaptiveRepair
            properties:
              active:
                default: true
                description: Active enables or disables adaptive repair for the cluster
                type: boolean
              clusterName:
                description: ClusterName is the name of the cluster this adaptive
                  repair configuration applies to
                minLength: 1
                type: string
              clusterType:
                description: ClusterType is the type of the cluster (cassandra, dse).
                  Kafka is not supported for adaptive repair.
                enum:
                - cassandra
                - dse
                type: string
              connectionRef:
                description: ConnectionRef is the name of an AxonOpsConnection in
                  the same namespace.
                minLength: 1
                type: string
              excludedTables:
                description: |-
                  ExcludedTables is a list of fully-qualified table names excluded from repair (e.g. keyspace.table).
                  Maps to the API field BlacklistedTables.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              filterTWCSTables:
                default: true
                description: FilterTWCSTables skips tables using TimeWindowCompactionStrategy
                  when set to true
                type: boolean
              gcGraceThreshold:
                default: 86400
                description: |-
                  GcGraceThreshold is the minimum gc_grace_seconds value for a table to be considered for repair.
                  Tables with gc_grace shorter than this value will not be repaired.
                format: int64
                minimum: 0
                type: integer
              maxSegmentsPerTable:
                default: 0
                description: MaxSegmentsPerTable is the maximum number of segments
                  per table. 0 means AxonOps decides automatically.
                format: int32
                minimum: 0
                type: integer
              segmentRetries:
                default: 3
                description: SegmentRetries is the maximum number of retries per repair
                  segment before marking it as failed
                format: int32
                minimum: 0
                type: integer
              segmentTargetSizeMB:
                default: 0
                description: SegmentTargetSizeMB is the target size in MB for each
                  repair segment. 0 means AxonOps decides automatically.
                format: int32
                minimum: 0
                type: integer
              segmentTimeout:
                default: 2h
                description: SegmentTimeout is the per-segment timeout duration string
                  (e.g. "2h", "30m")
                type: string
              tableParallelism:
                default: 10
                description: TableParallelism is the number of tables repaired concurrently
                format: int32
                minimum: 1
                type: integer
            required:
            - clusterName
            - clusterType
            - connectionRef
            type: object
          status:
            description: status defines the observed state of AxonOpsAdaptiveRepair
            properties:
              conditions:
                description: |-
                  Conditions represent the current state of the AxonOpsAdaptiveRepair resource.

                  Standard condition types include:
                  - "Ready": the adaptive repair settings are synced with AxonOps
                  - "Failed": the settings failed to sync with AxonOps

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime is the timestamp of the last successful
                  sync with AxonOps
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed AxonOpsAdaptiveRepair
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
