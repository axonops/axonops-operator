---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: axonopshealthcheckhttps.alerts.axonops.com
spec:
  group: alerts.axonops.com
  names:
    kind: AxonOpsHealthcheckHTTP
    listKind: AxonOpsHealthcheckHTTPList
    plural: axonopshealthcheckhttps
    singular: axonopshealthcheckhttp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - jsonPath: .spec.clusterType
      name: Type
      type: string
    - jsonPath: .spec.url
      name: URL
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AxonOpsHealthcheckHTTP is the Schema for the axonopshealthcheckhttps
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AxonOpsHealthcheckHTTP
            properties:
              body:
                description: Body is the HTTP request body (for POST, PUT, etc.)
                type: string
              clusterName:
                description: ClusterName is the name of the cluster in AxonOps
                minLength: 1
                type: string
              clusterType:
                description: ClusterType is the type of the cluster (cassandra, kafka,
                  dse)
                enum:
                - cassandra
                - kafka
                - dse
                type: string
              connectionRef:
                description: ConnectionRef is the name of an AxonOpsConnection in
                  the same namespace.
                minLength: 1
                type: string
              expectedStatus:
                default: 200
                description: ExpectedStatus is the expected HTTP response status code
                type: integer
              headers:
                additionalProperties:
                  type: string
                description: Headers is a map of HTTP headers to send with the request
                type: object
              interval:
                default: 1m
                description: Interval is the interval between checks (e.g., 1m, 30s)
                type: string
              method:
                default: GET
                description: Method is the HTTP method to use (GET, POST, PUT, DELETE,
                  etc.)
                type: string
              name:
                description: Name is the name of the healthcheck
                minLength: 1
                type: string
              readonly:
                default: false
                description: Readonly indicates whether the healthcheck is read-only
                type: boolean
              supportedAgentTypes:
                description: SupportedAgentTypes lists agent types this healthcheck
                  applies to
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              timeout:
                default: 1m
                description: Timeout is the timeout for the check (e.g., 1m, 30s)
                type: string
              url:
                description: URL is the HTTP URL to check
                minLength: 1
                type: string
            required:
            - clusterName
            - clusterType
            - connectionRef
            - name
            - url
            type: object
          status:
            description: status defines the observed state of AxonOpsHealthcheckHTTP
            properties:
              conditions:
                description: Conditions represent the current state of the AxonOpsHealthcheckHTTP
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime is the timestamp of the last successful
                  sync with AxonOps
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation observed by
                  the controller
                format: int64
                type: integer
              syncedHealthcheckID:
                description: SyncedHealthcheckID is the ID assigned by AxonOps API
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
